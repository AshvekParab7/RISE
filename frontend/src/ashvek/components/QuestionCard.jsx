export default function QuestionCard({ question, answer, setAnswer, onSubmit, onHelp, busy = false, feedback }) {
  if (!question) return null
  return <section className="ashvek-question panel"><div className="ashvek-question-label"><span>{question.type?.replaceAll('_', ' ') || 'YOUR TURN'}</span><small>Think before you answer</small></div><h3>{question.prompt}</h3>{question.options?.length > 0 && <div className="ashvek-options">{question.options.map(option => <button className={answer === option ? 'selected' : ''} onClick={() => setAnswer(option)} key={option}>{option}</button>)}</div>}{!question.options?.length && <textarea value={answer} onChange={event => setAnswer(event.target.value)} placeholder="Explain it in your own words..." rows={4}/>}<div className="ashvek-question-actions"><button className="button button-primary" onClick={() => onSubmit()} disabled={busy || !answer.trim()}>{busy ? 'Evaluating...' : 'Submit answer'}</button><button className="button ashvek-help-button" onClick={() => (onHelp || onSubmit)()} disabled={busy}>🤔 I don’t understand</button></div>{feedback && <div className={`ashvek-feedback ${feedback.correct ? 'correct' : 'needs-work'}`}><strong>{feedback.correct ? 'What you got right' : 'What needs work'}</strong><p>{feedback.feedback}</p>{feedback.missing_points?.length > 0 && <div><b>What you are missing</b>{feedback.missing_points.map(item => <span key={item}>• {item}</span>)}</div>}{feedback.misconception && <small>Correction: {feedback.misconception}</small>}{feedback.help_requested && <small>Next: a simpler diagnostic question</small>}</div>}</section>
}
