export default function SessionReport({ report, points, onPractice, onContinue }) {
  return <section className="ashvek-report panel"><p className="eyebrow">SESSION COMPLETE</p><h2>{report?.topic || 'Learning session'}</h2><div className="ashvek-report-score"><strong>{report?.understanding ?? report?.percentage ?? 0}%</strong><span>understanding</span><strong>{report?.correct ?? report?.score ?? 0}/{report?.questions ?? report?.total ?? 0}</strong><span>correct</span><strong>+{points ?? report?.learning_points ?? 0}</strong><span>learning points</span></div><div className="ashvek-report-columns"><div><b>✓ Strong</b>{(report?.strong_concepts || []).map(item => <span key={item}>{item}</span>)}</div><div><b>⚠ Needs practice</b>{(report?.weak_concepts || []).map(item => <span key={item}>{item}</span>)}</div></div>{report?.detected_misconception && <div className="ashvek-report-note"><b>What to remember</b><p>{report.detected_misconception}</p></div>}<p className="ashvek-next"><b>🎯 Recommended next</b>{report?.recommended_next_step}</p><div className="button-row"><button className="button button-primary" onClick={onPractice}>Practice weak area</button><button className="button" onClick={onContinue}>Back to Study Coach</button></div></section>
}
